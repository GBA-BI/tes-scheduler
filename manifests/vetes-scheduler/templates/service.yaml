apiVersion: v1
kind: Service
metadata:
  name: {{ include "vetes-scheduler.fullname" . }}
  labels:
    {{- include "vetes-scheduler.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vetes-scheduler.selectorLabels" . | nindent 4 }}
